import './sketchMain.js';

/*
import('./sketchMain.js').then(module => {
  console.log(module)
});
*/
/*

.then(module => {
      // 動的に読み込まれたSubクラス
      const sub = new module.Sub();
      sub.subMethod();  
    });
*/
